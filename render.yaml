services:
  - type: web
    name: crypto-orderbook
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm run build
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "20"
